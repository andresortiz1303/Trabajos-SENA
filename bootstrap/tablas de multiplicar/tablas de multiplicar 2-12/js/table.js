


function printTable() {
    let tab = 1;
    let tabF = 12;
    let num = 10;
    let result1 = 0;
    let result2 = 0;
    let result3 = 0;
    let odd = 0;

    while (tab < tabF) {

        let cont = 0;

        tab = tab + 1;

        // while (cont < num) {
        //     cont = cont + 1;
        // let mult = cont * tab;

        if(tab==2){
            while (cont < num) {
            cont = cont + 1;
            mult = cont * tab;
            if (mult % 2 == 0) {
            odd = odd + 1
            }

            if (mult > 10 & mult < 20) {
            result1 = result1 + 1
            } if (mult > 20 & mult < 50) {
            result2 = result2 + 1
             } if (mult > 50) {
                 result3 = result3 + 1
             }
            document.getElementById("table-2").innerHTML += (tab + "x" + cont + "=" + mult + "<br>");
             }

        } else if(tab==3){
            while (cont < num) {
            cont = cont + 1;
            mult = cont * tab;

            if (mult % 2 == 0) {
            odd = odd + 1
            }

            if (mult > 10 & mult < 20) {
            result1 = result1 + 1
            } if (mult > 20 & mult < 50) {
            result2 = result2 + 1
             } if (mult > 50) {
                 result3 = result3 + 1
             }
           document.getElementById("table-3").innerHTML += (tab + "x" + cont + "=" + mult + "<br>");
        }

        } else if(tab==4){
            while (cont < num) {
            cont = cont + 1;
            mult = cont * tab;

            if (mult % 2 == 0) {
            odd = odd + 1
            }

            if (mult > 10 & mult < 20) {
            result1 = result1 + 1
            } if (mult > 20 & mult < 50) {
            result2 = result2 + 1
             } if (mult > 50) {
                 result3 = result3 + 1
             }
           document.getElementById("table-4").innerHTML += (tab + "x" + cont + "=" + mult + "<br>");
        }
        } else if(tab==5){
            while (cont < num) {
            cont = cont + 1;
            mult = cont * tab;

            if (mult % 2 == 0) {
            odd = odd + 1
            }

            if (mult > 10 & mult < 20) {
            result1 = result1 + 1
            } if (mult > 20 & mult < 50) {
            result2 = result2 + 1
             } if (mult > 50) {
                 result3 = result3 + 1
             }
           document.getElementById("table-5").innerHTML += (tab + "x" + cont + "=" + mult + "<br>");
        }
        } else if(tab==6){
            while (cont < num) {
            cont = cont + 1;
            mult = cont * tab;

            if (mult % 2 == 0) {
            odd = odd + 1
            }

            if (mult > 10 & mult < 20) {
            result1 = result1 + 1
            } if (mult > 20 & mult < 50) {
            result2 = result2 + 1
             } if (mult > 50) {
                 result3 = result3 + 1
             }
           document.getElementById("table-6").innerHTML += (tab + "x" + cont + "=" + mult + "<br>");
        }
        } else if(tab==7){
            while (cont < num) {
            cont = cont + 1;
            mult = cont * tab;

            if (mult % 2 == 0) {
            odd = odd + 1
            }

            if (mult > 10 & mult < 20) {
            result1 = result1 + 1
            } if (mult > 20 & mult < 50) {
            result2 = result2 + 1
             } if (mult > 50) {
                 result3 = result3 + 1
             }
           document.getElementById("table-7").innerHTML += (tab + "x" + cont + "=" + mult + "<br>");
        }
        } else if(tab==8){
            while (cont < num) {
            cont = cont + 1;
            mult = cont * tab;

            if (mult % 2 == 0) {
            odd = odd + 1
            }

            if (mult > 10 & mult < 20) {
            result1 = result1 + 1
            } if (mult > 20 & mult < 50) {
            result2 = result2 + 1
             } if (mult > 50) {
                 result3 = result3 + 1
             }
           document.getElementById("table-8").innerHTML += (tab + "x" + cont + "=" + mult + "<br>");
        }
        } else if(tab==9){
            while (cont < num) {
            cont = cont + 1;
            mult = cont * tab;

            if (mult % 2 == 0) {
            odd = odd + 1
            }

            if (mult > 10 & mult < 20) {
            result1 = result1 + 1
            } if (mult > 20 & mult < 50) {
            result2 = result2 + 1
             } if (mult > 50) {
                 result3 = result3 + 1
             }
           document.getElementById("table-9").innerHTML += (tab + "x" + cont + "=" + mult + "<br>");
        }
        } else if(tab==10){
            while (cont < num) {
            cont = cont + 1;
            mult = cont * tab;

            if (mult % 2 == 0) {
            odd = odd + 1
            }

            if (mult > 10 & mult < 20) {
            result1 = result1 + 1
            } if (mult > 20 & mult < 50) {
            result2 = result2 + 1
             } if (mult > 50) {
                 result3 = result3 + 1
             }
           document.getElementById("table-10").innerHTML += (tab + "x" + cont + "=" + mult + "<br>");
        }
        } else if(tab==11){
            while (cont < num) {
            cont = cont + 1;
            mult = cont * tab;

            if (mult % 2 == 0) {
            odd = odd + 1
            }

            if (mult > 10 & mult < 20) {
            result1 = result1 + 1
            } if (mult > 20 & mult < 50) {
            result2 = result2 + 1
             } if (mult > 50) {
                 result3 = result3 + 1
             }
           document.getElementById("table-11").innerHTML += (tab + "x" + cont + "=" + mult + "<br>");
        }
        } else if(tab==12){
            while (cont < num) {
            cont = cont + 1;
            mult = cont * tab;

            if (mult % 2 == 0) {
            odd = odd + 1
            }

            if (mult > 10 & mult < 20) {
            result1 = result1 + 1
            } if (mult > 20 & mult < 50) {
            result2 = result2 + 1
             } if (mult > 50) {
                 result3 = result3 + 1
             }
           document.getElementById("table-12").innerHTML += (tab + "x" + cont + "=" + mult + "<br>");
        }

           
        }
    }
    document.getElementById("odd").innerHTML = "odd: " +odd;
    document.getElementById("The number of numbers between 10 and 20 are: ").innerHTML = "The number of numbers between 10 and 20 are: " +result1;
    document.getElementById("The number of numbers between 20 and 50 are: ").innerHTML = "The number of numbers between 20 and 50 are: " +result2;
    document.getElementById("The number of numbers between 50: ").innerHTML = "The number of numbers between 50: " +result3;

}


